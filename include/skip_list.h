template <typename T>
class SkipList 
{
    public:
        // these methods are just dummies for correct set up of CI
        SkipList() = default;
        ~SkipList() = default;

        void doNothing(const T& value) 
        {
            (void)value;
        }
};